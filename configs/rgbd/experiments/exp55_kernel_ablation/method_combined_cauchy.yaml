# EXP55 single-variable intervention: combined-prune with RobustTracking.kernel
# huber -> cauchy. delta stays 0.10 on both channels; everything else is the
# main-table combined arm identity (method_combined_maskboth_prune.yaml).
inherit_from: "configs/rgbd/experiments/active/candidate/method_combined_maskboth_prune.yaml"

RobustTracking:
  kernel: "cauchy"
