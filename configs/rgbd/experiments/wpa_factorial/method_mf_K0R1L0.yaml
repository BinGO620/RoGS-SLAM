# WP-A FACTORIAL method overlay — arm K0R1L0 (mask-free, K+L OFF, R ON).
# 2026-08-14, WP-A. Lower-order cell for the descriptive full table / interaction structure.
inherit_from: "configs/rgbd/experiments/active/candidate/method_combined_maskoff_prune.yaml"

TriReliability:
  enabled: false

RobustTracking:
  enabled: true

DynamicKeyframe:
  enabled: false

ReliabilitySignal:
  enabled: false

SemanticMask:
  enabled: false

DeferredCommit:
  enabled: true
  reliability_confirm: true

Mapping:
  lifecycle_mode: "prune"
