# WP-A FACTORIAL run config — balloon, arm K0R1L0 (mask-free, K=0 R=1 L=0).
# Contract pinned by tests/test_wpa_factorial_configs.py (all cells share the mask-free
# skeleton + fixed constants; only the three booleans vary).
inherit_from: "configs/rgbd/bonn/balloon.yaml"
method_from: "configs/rgbd/experiments/wpa_factorial/method_mf_K0R1L0.yaml"
method: "WPA-balloon-KK0R1L0"
