# WP-A FACTORIAL method overlay — arm K1R0L1 (mask-free, R OFF, K+L ON).
# 2026-08-14, WP-A. Δ_R = log(ATE_K1R0L1 / ATE_K1R1L1): cost of removing R at full config.
# RobustTracking off = vanilla (non-robust) kernel; K+L on, mask-free.
inherit_from: "configs/rgbd/experiments/active/candidate/method_combined_maskoff_prune.yaml"

TriReliability:
  enabled: false

RobustTracking:
  enabled: false

DynamicKeyframe:
  enabled: true

ReliabilitySignal:
  enabled: true

SemanticMask:
  enabled: false

DeferredCommit:
  enabled: true
  reliability_confirm: true

Mapping:
  lifecycle_mode: "prune"
