# P-B 2x2 arm: mask ON, DynamicKeyframe OFF (prune lifecycle). Everything else = combined.
inherit_from: "configs/rgbd/experiments/active/candidate/method_combined_maskboth_prune.yaml"
DynamicKeyframe:
  enabled: false
