# WP-M MASK-ONLY run config -- pt1. Arm = combined main-table overlay with
# the three kernel components (K/R/L) OFF; only diff vs combined = {K,R,L}.
# Contract pinned by tests/test_wpm_maskonly_configs.py.
inherit_from: "configs/rgbd/bonn/person_tracking.yaml"
method_from: "configs/rgbd/experiments/wpm_maskonly/method_maskonly.yaml"
method: "WPM-pt1-maskonly"
