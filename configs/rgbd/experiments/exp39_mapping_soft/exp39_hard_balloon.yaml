# exp39 Phase 0 / 对照臂 H —— 现状硬 mask（floor 恒等于 0）。
# 与治疗臂 W 的唯一差异 = SemanticMask.soft_mapping（由 tests/test_exp39_phase0_configs.py 钉住）。
# MappingProbe 在两臂都开：H 臂的动态梯度份额必须恰好为 0（预注册 G-2 负对照）。
inherit_from: "configs/rgbd/experiments/t2_mad_quota/t2_eboth_balloon.yaml"
method: "EXP39-hard-balloon"

MappingProbe:
  enabled: true
  interval: 50
