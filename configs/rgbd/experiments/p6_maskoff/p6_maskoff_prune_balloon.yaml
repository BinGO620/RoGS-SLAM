# P6 MASK-OFF 消融 run config — balloon prune, SemanticMask off.
# That which isolates the mask: EVERYTHING else byte-identical to p2s_combined_prune_balloon.yaml
# (combined prune backbone), except method_from points at method_combined_maskoff_prune.yaml
# (= combined prune with SemanticMask.enabled: false, pinned by tests/test_p6_maskoff_configs.py).
# Self-tracked, single seed => screening only (discipline ⑤).
inherit_from: "configs/rgbd/bonn/balloon.yaml"
method_from: "configs/rgbd/experiments/active/candidate/method_combined_maskoff_prune.yaml"
method: "Combined-MaskOff"
